import { StyleSheet } from "react-native";

export const Styles = StyleSheet.create({
  container: {
    position: "absolute",
    top: 10,
    right: 10,
    zIndex: 2,
  },
  inner: {
    width: 30,
    height: 30,
    padding: 5,
    backgroundColor: "#fff",
  },
});
